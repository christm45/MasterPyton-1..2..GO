// ──────────────────────────────────────────────────────────
// PYTHON LIBRARIES TRACK - 10 Quizzes (8 Questions Each)
// Deep explanations for every answer
// ──────────────────────────────────────────────────────────

export const libraryQuizzes = [
  {
    id: 'qlib01', lessonId: 'lib01', title: 'math & random Quiz', difficulty: 'library',
    questions: [
      { question: "What does math.ceil(4.1) return?", options: ["4", "5", "4.1", "4.0"], correct: 1, explanation: "math.ceil() always rounds UP to the nearest integer. 4.1 rounds up to 5. In contrast, math.floor() rounds down and would return 4." },
      { question: "What is math.sqrt(144)?", options: ["12.0", "12", "144", "Error"], correct: 0, explanation: "math.sqrt() returns the square root as a float. The square root of 144 is 12.0. Use math.isqrt(144) if you want the integer 12." },
      { question: "What does random.choice(['a','b','c']) do?", options: ["Returns one random element", "Returns all elements shuffled", "Returns 3 random elements", "Returns the first element"], correct: 0, explanation: "random.choice() picks and returns a single random element from a sequence. For multiple picks, use random.choices(k=n) or random.sample(k=n)." },
      { question: "What is the purpose of random.seed(42)?", options: ["Makes random results reproducible", "Generates the number 42", "Seeds the list with 42 items", "Resets random to default"], correct: 0, explanation: "Setting a seed makes the sequence of random numbers deterministic. With seed(42), you'll always get the same 'random' numbers in the same order — essential for testing and debugging." },
      { question: "What does math.isclose(0.1+0.2, 0.3) return?", options: ["True", "False", "0.3", "Error"], correct: 0, explanation: "math.isclose() compares floats with a tolerance. 0.1+0.2 actually equals 0.30000000000000004 due to floating-point arithmetic, so == returns False, but isclose() correctly returns True." },
      { question: "What is the difference between random.choices() and random.sample()?", options: ["choices allows repeats, sample doesn't", "sample allows repeats, choices doesn't", "They are identical", "choices returns strings, sample returns numbers"], correct: 0, explanation: "random.choices(seq, k=n) picks WITH replacement (items can repeat). random.sample(seq, k=n) picks WITHOUT replacement (each item at most once). sample raises ValueError if k > len(seq)." },
      { question: "What does math.log(100, 10) return?", options: ["2.0", "10.0", "100.0", "1.0"], correct: 0, explanation: "math.log(x, base) computes the logarithm. log base 10 of 100 = 2.0, because 10^2 = 100. Without a base argument, math.log() computes the natural log (base e)." },
      { question: "What does random.shuffle(my_list) return?", options: ["None (shuffles in-place)", "A new shuffled list", "The original list", "A random element"], correct: 0, explanation: "random.shuffle() modifies the list in-place and returns None. It does NOT return a new list. If you need a new shuffled list without modifying the original, use random.sample(my_list, k=len(my_list))." }
    ]
  },
  {
    id: 'qlib02', lessonId: 'lib02', title: 'datetime Quiz', difficulty: 'library',
    questions: [
      { question: "What does datetime.now() return?", options: ["Current date and time", "Current date only", "Current time only", "Unix timestamp"], correct: 0, explanation: "datetime.now() returns a datetime object with the current date AND time (year, month, day, hour, minute, second, microsecond). Use date.today() for date only." },
      { question: "What does strftime stand for?", options: ["String format time", "Structure format time", "Standard format time", "Stream format time"], correct: 0, explanation: "strftime = 'string format time'. It converts a datetime object INTO a formatted string. Its counterpart strptime = 'string parse time' converts a string INTO a datetime object." },
      { question: "What does timedelta(days=7) represent?", options: ["A duration of 7 days", "A specific date 7 days ago", "The 7th day of the year", "7 milliseconds"], correct: 0, explanation: "timedelta represents a duration — a difference between two dates/times. timedelta(days=7) is just 'seven days'. Add it to a datetime to get a future date: datetime.now() + timedelta(days=7)." },
      { question: "What format code gives the full month name?", options: ["%B", "%m", "%b", "%M"], correct: 0, explanation: "%B gives the full month name (e.g., 'March'). %b gives abbreviated ('Mar'), %m gives zero-padded number ('03'), and %M is for minutes, not months." },
      { question: "Why doesn't timedelta support months?", options: ["Months vary in length (28-31 days)", "It's a bug in Python", "You need a different module", "Months are not a unit of time"], correct: 0, explanation: "timedelta doesn't support months because months have variable lengths (28, 29, 30, or 31 days). Adding '1 month' to January 31 is ambiguous — should it be February 28 or March 3? For month arithmetic, use the dateutil library." },
      { question: "What does date.today().weekday() return for Monday?", options: ["0", "1", "7", "'Monday'"], correct: 0, explanation: "weekday() returns 0 for Monday through 6 for Sunday. This follows the ISO standard. If you want 1-7 (Mon-Sun), use isoweekday() instead." },
      { question: "How do you parse '15/03/2025' into a datetime?", options: ["strptime(s, '%d/%m/%Y')", "strftime(s, '%d/%m/%Y')", "datetime(s)", "parse(s)"], correct: 0, explanation: "datetime.strptime(string, format) parses a string into a datetime. The format must match exactly: %d=day, %m=month, %Y=4-digit year, with / as separators. strftime goes the other direction (datetime to string)." },
      { question: "What does (date2 - date1).days give you?", options: ["Integer number of days between dates", "A timedelta object", "A float", "An error"], correct: 0, explanation: "Subtracting two dates gives a timedelta. Accessing .days on that timedelta gives you an integer — the number of days between the two dates. The result is positive if date2 > date1, negative otherwise." }
    ]
  },
  {
    id: 'qlib03', lessonId: 'lib03', title: 'string & textwrap Quiz', difficulty: 'library',
    questions: [
      { question: "What does string.ascii_letters contain?", options: ["All uppercase and lowercase letters", "Only lowercase letters", "Letters and digits", "Only uppercase letters"], correct: 0, explanation: "string.ascii_letters is the concatenation of ascii_lowercase ('abcdefghijklmnopqrstuvwxyz') and ascii_uppercase ('ABCDEFGHIJKLMNOPQRSTUVWXYZ'). It contains all 52 ASCII letters." },
      { question: "What is the advantage of Template strings over f-strings?", options: ["Safer for user-provided templates", "Faster execution", "More features", "Better formatting"], correct: 0, explanation: "Template strings are safer because they only support simple $variable substitution and cannot execute arbitrary Python expressions. F-strings can run any code: f'{__import__(\"os\").system(\"rm -rf /\")}'. Never use f-strings with untrusted input." },
      { question: "What does textwrap.fill(text, width=40) do?", options: ["Wraps text to 40 chars per line as a string", "Truncates text to 40 chars", "Pads text to 40 chars", "Splits into list of 40-char lines"], correct: 0, explanation: "textwrap.fill() wraps long text by inserting newlines, returning a single string. For a list of lines instead, use textwrap.wrap(). width controls the maximum line length." },
      { question: "What does textwrap.dedent() do?", options: ["Removes common leading whitespace", "Adds indentation", "Removes all whitespace", "Removes trailing spaces"], correct: 0, explanation: "textwrap.dedent() removes common leading whitespace from all lines. If every line starts with 4 spaces, it removes those 4 spaces from each line. Useful for cleaning up multi-line strings defined inside functions." },
      { question: "What does Template('$name').safe_substitute() do with missing keys?", options: ["Keeps the $placeholder as-is", "Raises KeyError", "Replaces with empty string", "Replaces with None"], correct: 0, explanation: "safe_substitute() is the forgiving version. Missing keys are left as '$variable' in the output. Regular substitute() would raise a KeyError for any missing key." },
      { question: "Which string constant contains '!@#$%'?", options: ["string.punctuation", "string.printable", "Both", "Neither"], correct: 2, explanation: "string.punctuation contains all ASCII punctuation characters including !@#$%. string.printable contains ALL printable characters (letters + digits + punctuation + whitespace), so it includes them too." },
      { question: "What does textwrap.shorten('Hello World Foo', width=12) produce?", options: ["'Hello [...]'", "'Hello World'", "'Hello Wor...'", "'Hello World '"], correct: 0, explanation: "textwrap.shorten() collapses whitespace and truncates to fit within width, adding a placeholder (default '[...]'). It breaks at word boundaries. Use placeholder='...' to customize the truncation indicator." },
      { question: "How do you check if a character is a digit using the string module?", options: ["char in string.digits", "string.isdigit(char)", "char.isdigit()", "Both A and C work"], correct: 3, explanation: "Both 'char in string.digits' and 'char.isdigit()' check for digits, but they differ slightly: string.digits only contains '0123456789', while str.isdigit() also matches Unicode digits like '²'. For ASCII-only validation, string.digits is more precise." }
    ]
  },
  {
    id: 'qlib04', lessonId: 'lib04', title: 'collections Quiz', difficulty: 'library',
    questions: [
      { question: "What does Counter('hello') produce?", options: ["Counter({'l': 2, 'h': 1, 'e': 1, 'o': 1})", "Counter({'h': 1, 'e': 1, 'l': 1, 'o': 1})", "5", "{'hello': 1}"], correct: 0, explanation: "Counter counts each element's occurrences. In 'hello', 'l' appears 2 times, while 'h', 'e', and 'o' each appear once. Counter works on any iterable — strings, lists, etc." },
      { question: "What happens when you access a missing key in Counter?", options: ["Returns 0", "Raises KeyError", "Returns None", "Returns -1"], correct: 0, explanation: "Counter returns 0 for missing keys instead of raising KeyError. This is because Counter represents counts, and a missing element has been counted 0 times. This makes Counter safe to use without key existence checks." },
      { question: "What is the main advantage of defaultdict over regular dict?", options: ["Auto-creates default values for missing keys", "It's faster", "It's sorted", "It uses less memory"], correct: 0, explanation: "defaultdict automatically creates a default value when accessing a missing key. defaultdict(list) creates an empty list, defaultdict(int) creates 0, defaultdict(str) creates ''. This eliminates the need for 'if key not in dict' checks." },
      { question: "Why use namedtuple instead of a regular tuple?", options: ["Access elements by name instead of index", "It's faster", "It uses less memory", "It's mutable"], correct: 0, explanation: "namedtuple lets you access elements by meaningful names (point.x, point.y) instead of cryptic indices (point[0], point[1]). This makes code self-documenting. namedtuples are still immutable and memory-efficient like regular tuples." },
      { question: "Why is deque faster than list for left-side operations?", options: ["O(1) for append/pop on both ends", "It uses C implementation", "It has less overhead", "It's not actually faster"], correct: 0, explanation: "list.insert(0, x) and list.pop(0) are O(n) because all elements must shift. deque uses a doubly-linked list internally, giving O(1) performance for appendleft() and popleft(). For queue/stack patterns, deque is far superior." },
      { question: "What does deque(maxlen=5) do when you add a 6th element?", options: ["Removes the oldest element", "Raises an error", "Ignores the new element", "Doubles the size"], correct: 0, explanation: "A bounded deque automatically removes the oldest element from the opposite end when full. Appending to a full deque removes from the left; appendleft to a full deque removes from the right. Perfect for 'last N items' patterns." },
      { question: "What does Counter.most_common(2) return?", options: ["List of 2 most frequent (element, count) pairs", "The 2 highest counts", "2 random elements", "The 2 least common"], correct: 0, explanation: "most_common(n) returns a list of the n most common elements and their counts, from most to least common. Without an argument, most_common() returns ALL elements ordered by frequency." },
      { question: "What does ChainMap do?", options: ["Searches multiple dicts in order, first match wins", "Combines dicts into one", "Chains dict keys together", "Creates a linked list of dicts"], correct: 0, explanation: "ChainMap groups multiple dicts and searches them in order. The first dict containing the key wins. It doesn't merge the dicts — they remain separate, so updates to underlying dicts are reflected. Great for layered settings (user prefs > defaults)." }
    ]
  },
  {
    id: 'qlib05', lessonId: 'lib05', title: 'itertools Quiz', difficulty: 'library',
    questions: [
      { question: "What does itertools.chain([1,2], [3,4]) produce?", options: ["[1, 2, 3, 4]", "[[1,2], [3,4]]", "[1, 3, 2, 4]", "[(1,3), (2,4)]"], correct: 0, explanation: "chain() connects multiple iterables end-to-end into a single sequence. chain([1,2], [3,4]) yields 1, 2, 3, 4 in order. It's like flattening one level of nesting, more efficient than list concatenation with +." },
      { question: "How many items does combinations('ABCD', 2) produce?", options: ["6", "4", "8", "12"], correct: 0, explanation: "combinations chooses subsets where order doesn't matter. From 4 items choosing 2: C(4,2) = 4!/(2!*2!) = 6. The pairs are: AB, AC, AD, BC, BD, CD. Unlike permutations, AB and BA are considered the same combination." },
      { question: "What is the difference between permutations and combinations?", options: ["Permutations care about order, combinations don't", "Combinations care about order, permutations don't", "They are the same thing", "Permutations only work with numbers"], correct: 0, explanation: "Permutations: order matters (AB != BA). Combinations: order doesn't matter (AB == BA). permutations('AB', 2) gives ('A','B') AND ('B','A'). combinations('AB', 2) gives only ('A','B')." },
      { question: "What does itertools.product('AB', '12') produce?", options: ["All pairs: A1, A2, B1, B2", "['AB12']", "['A1', 'B2']", "['AB', '12']"], correct: 0, explanation: "product() computes the Cartesian product — every combination of one element from each iterable. With 'AB' and '12', you get: ('A','1'), ('A','2'), ('B','1'), ('B','2'). It's equivalent to nested for loops." },
      { question: "Why must data be sorted before using groupby()?", options: ["groupby only groups consecutive identical keys", "It raises an error otherwise", "For performance reasons", "It's a Python requirement"], correct: 0, explanation: "groupby() groups consecutive elements with the same key. If data is [A, B, A], groupby gives three groups: [A], [B], [A] — not two groups [A,A] and [B]. You must sort first so identical keys are adjacent." },
      { question: "What does accumulate([1,2,3,4,5]) produce?", options: ["[1, 3, 6, 10, 15]", "[15]", "[1, 2, 3, 4, 5]", "[5, 4, 3, 2, 1]"], correct: 0, explanation: "accumulate() produces running (cumulative) results. By default it sums: 1, 1+2=3, 3+3=6, 6+4=10, 10+5=15. You can pass a different function like operator.mul for running products, or max for running maximums." },
      { question: "What does takewhile(lambda x: x<5, [1,3,6,2,4]) return?", options: ["[1, 3]", "[1, 3, 2, 4]", "[1, 3, 6]", "[6, 2, 4]"], correct: 0, explanation: "takewhile() yields elements UNTIL the predicate becomes False, then stops completely. It takes 1 (1<5: True), 3 (3<5: True), then hits 6 (6<5: False) and stops. It does NOT look at 2 and 4, even though they're < 5." },
      { question: "What does islice(range(100), 5, 10) produce?", options: ["[5, 6, 7, 8, 9]", "[0, 1, 2, 3, 4]", "range(5, 10)", "[5, 10]"], correct: 0, explanation: "islice() works like list slicing but on any iterator. islice(iterable, start, stop) yields elements from index start to stop-1. islice(range(100), 5, 10) yields elements at indices 5, 6, 7, 8, 9. It's memory-efficient because it doesn't create a full list." }
    ]
  },
  {
    id: 'qlib06', lessonId: 'lib06', title: 'functools & operator Quiz', difficulty: 'library',
    questions: [
      { question: "What does @lru_cache do?", options: ["Caches function results for repeated calls", "Makes functions run in parallel", "Limits function execution time", "Logs function calls"], correct: 0, explanation: "lru_cache (Least Recently Used cache) stores function results. If the same arguments are passed again, it returns the cached result instantly instead of recomputing. This turns exponential recursive algorithms into linear ones." },
      { question: "Why is lru_cache effective for recursive Fibonacci?", options: ["Avoids recomputing the same values", "It runs in parallel", "It uses less memory", "It changes the algorithm"], correct: 0, explanation: "Without caching, fib(50) makes ~2^50 calls because it recomputes fib(48), fib(47), etc. thousands of times. With lru_cache, each fib(n) is computed only ONCE and then returned from cache. This changes O(2^n) to O(n)." },
      { question: "What does partial(pow, 2) create?", options: ["A function that computes 2^x", "A function that computes x^2", "The value 2", "An error"], correct: 0, explanation: "partial(pow, 2) fills in the FIRST argument of pow. So calling the result with x computes pow(2, x) = 2^x. If you wanted x^2, you'd need partial(pow, exponent=2) — but pow() doesn't have keyword args, so you'd use a lambda instead." },
      { question: "What does reduce(lambda a,b: a+b, [1,2,3,4]) compute?", options: ["10 (sum of all elements)", "4 (last element)", "[1, 2, 3, 4]", "Error"], correct: 0, explanation: "reduce applies the function cumulatively: ((1+2)+3)+4 = 10. Step by step: a=1,b=2 -> 3, then a=3,b=3 -> 6, then a=6,b=4 -> 10. It 'reduces' the list to a single value." },
      { question: "What is operator.itemgetter('name') used for?", options: ["Creates a function that gets the 'name' key from dicts", "Gets an item from a list", "Creates a named operator", "Checks if 'name' exists"], correct: 0, explanation: "operator.itemgetter('name') returns a callable that extracts the 'name' key from any object. It's commonly used as a key function for sorting: sorted(users, key=itemgetter('name')). It's faster than lambda x: x['name']." },
      { question: "What does fibonacci.cache_info() return?", options: ["Cache statistics: hits, misses, size, maxsize", "The cached values", "True if cache is full", "The cache size only"], correct: 0, explanation: "cache_info() returns a named tuple with: hits (cache used), misses (computed fresh), maxsize (cache limit), currsize (current cached items). Use this to verify caching is working and tune maxsize." },
      { question: "What does @total_ordering help with?", options: ["Auto-generates all comparison methods from __eq__ and one other", "Orders function parameters", "Sorts class instances", "Orders method execution"], correct: 0, explanation: "@total_ordering auto-generates __le__, __gt__, __ge__ from just __eq__ and one ordering method (__lt__, __gt__, __le__, or __ge__). Without it, you'd need to implement all 6 comparison methods manually." },
      { question: "What happens if you use lru_cache with a list argument?", options: ["TypeError: unhashable type 'list'", "It works normally", "The list is converted to tuple", "The cache is disabled"], correct: 0, explanation: "lru_cache uses a dict internally, so arguments must be hashable (usable as dict keys). Lists are mutable and unhashable, so passing one raises TypeError. Convert lists to tuples first: func(tuple(my_list))." }
    ]
  },
  {
    id: 'qlib07', lessonId: 'lib07', title: 'Regular Expressions Quiz', difficulty: 'library',
    questions: [
      { question: "What does \\d+ match?", options: ["One or more digits", "Exactly one digit", "Zero or more digits", "The literal text '\\d+'"], correct: 0, explanation: "\\d matches a single digit (0-9). The + quantifier means 'one or more'. So \\d+ matches one or more consecutive digits: '1', '42', '12345'. For zero or more, use \\d*." },
      { question: "What is the difference between re.search() and re.match()?", options: ["search finds match anywhere, match only at start", "match finds all, search finds first", "They are identical", "search is faster"], correct: 0, explanation: "re.match() only checks at the START of the string. re.search() scans the entire string for the first match anywhere. re.match(r'\\d+', 'abc123') returns None, but re.search(r'\\d+', 'abc123') finds '123'." },
      { question: "What does re.findall(r'\\b\\w+\\b', 'Hello World') return?", options: ["['Hello', 'World']", "['H', 'e', 'l', 'l', 'o', ' ', 'W', 'o', 'r', 'l', 'd']", "['Hello World']", "['HelloWorld']"], correct: 0, explanation: "\\b is a word boundary, \\w+ matches one or more word characters. Together, \\b\\w+\\b matches complete words. findall() returns all matches as a list: ['Hello', 'World']." },
      { question: "What does the r prefix in r'\\d+' do?", options: ["Creates a raw string (backslashes are literal)", "Makes the regex case-insensitive", "Enables recursive matching", "Makes it run faster"], correct: 0, explanation: "The r prefix creates a raw string where backslashes are treated literally. Without it, Python interprets '\\d' as an escape sequence before the regex engine sees it. Always use raw strings for regex patterns." },
      { question: "What does re.sub(r'\\d+', 'X', 'a1b2c3') produce?", options: ["'aXbXcX'", "'XXX'", "'a1b2c3'", "'abc'"], correct: 0, explanation: "re.sub() replaces all matches of the pattern. \\d+ matches '1', '2', '3' separately, and each is replaced with 'X'. The non-matching characters 'a', 'b', 'c' remain, giving 'aXbXcX'." },
      { question: "What do parentheses () do in regex?", options: ["Create capture groups", "Make the pattern optional", "Increase priority", "Escape special characters"], correct: 0, explanation: "Parentheses create capture groups that extract parts of the match. In r'(\\w+)@(\\w+)', group(1) gives the username and group(2) gives the domain. Use (?:...) for non-capturing groups when you just need grouping without capturing." },
      { question: "What does the ? quantifier mean?", options: ["Zero or one (optional)", "One or more", "Zero or more", "Exactly one"], correct: 0, explanation: "? means 'zero or one' — makes the preceding element optional. colou?r matches both 'color' and 'colour'. When placed after * or + (like .*?), it makes them non-greedy (match as few characters as possible)." },
      { question: "What does re.compile() do and why use it?", options: ["Pre-compiles pattern for faster repeated use", "Checks if pattern is valid", "Converts string to regex", "Combines multiple patterns"], correct: 0, explanation: "re.compile() pre-compiles a regex pattern into a pattern object. This is faster when using the same pattern many times because the pattern is parsed and optimized only once. The compiled object has the same methods: .search(), .findall(), etc." }
    ]
  },
  {
    id: 'qlib08', lessonId: 'lib08', title: 'json & csv Quiz', difficulty: 'library',
    questions: [
      { question: "What does json.dumps() do?", options: ["Converts Python object to JSON string", "Converts JSON string to Python object", "Writes JSON to a file", "Reads JSON from a file"], correct: 0, explanation: "json.dumps() (dump string) serializes a Python object to a JSON-formatted string. json.loads() does the reverse. json.dump() writes to a file, json.load() reads from a file. Remember: 's' = string, no 's' = file." },
      { question: "What Python value does JSON 'null' become?", options: ["None", "0", "False", "'null'"], correct: 0, explanation: "JSON null maps to Python None. Other mappings: JSON true/false -> Python True/False, JSON arrays -> Python lists, JSON objects -> Python dicts. JSON numbers become int or float depending on whether they have a decimal point." },
      { question: "Why use csv.DictReader instead of csv.reader?", options: ["Access columns by name instead of index", "It's faster", "It handles larger files", "It auto-detects delimiters"], correct: 0, explanation: "DictReader uses the header row to create dictionaries for each row, so you access columns by name: row['name'] instead of row[0]. This is more readable and doesn't break if columns are reordered." },
      { question: "What does json.dumps(data, indent=2) do?", options: ["Pretty-prints JSON with 2-space indentation", "Limits JSON to 2 levels deep", "Adds 2 spaces before each key", "Truncates to 2 items"], correct: 0, explanation: "indent=2 produces human-readable JSON with newlines and 2-space indentation. Without indent, json.dumps() produces compact, single-line JSON. Use indent for config files and debugging; omit it for API responses and storage." },
      { question: "Can you serialize a Python set with json.dumps()?", options: ["No, sets are not JSON serializable by default", "Yes, it becomes a JSON array", "Yes, it becomes a JSON object", "Yes, it becomes a JSON string"], correct: 0, explanation: "Sets, datetime objects, and custom classes are NOT JSON serializable by default. json.dumps({1,2,3}) raises TypeError. You need a custom encoder (JSONEncoder subclass) or convert to list first: json.dumps(list(my_set))." },
      { question: "What does csv.writer use as the default delimiter?", options: ["Comma (,)", "Tab (\\t)", "Semicolon (;)", "Pipe (|)"], correct: 0, explanation: "By default, csv.writer uses comma as the delimiter (CSV = Comma Separated Values). You can change it: csv.writer(file, delimiter='\\t') for tab-separated, csv.writer(file, delimiter=';') for semicolons." },
      { question: "What is the correct way to read a CSV with headers?", options: ["csv.DictReader(file)", "csv.reader(file, headers=True)", "csv.read_headers(file)", "csv.parse(file)"], correct: 0, explanation: "csv.DictReader(file) automatically uses the first row as column headers and returns each subsequent row as an OrderedDict/dict. It's the most convenient way to work with CSV files that have headers." },
      { question: "Why use the csv module instead of splitting by commas?", options: ["Handles quoted fields and commas within values", "It's faster", "It validates data types", "Government regulations require it"], correct: 0, explanation: "CSV can have values containing commas: '\"New York, NY\",10001'. Naive split(',') would break this. The csv module correctly handles: quoted fields, embedded commas, embedded newlines, and different line endings." }
    ]
  },
  {
    id: 'qlib09', lessonId: 'lib09', title: 'statistics & decimal Quiz', difficulty: 'library',
    questions: [
      { question: "What does statistics.mean([2, 4, 6, 8]) return?", options: ["5.0", "4", "6", "20"], correct: 0, explanation: "mean() calculates the arithmetic average: (2+4+6+8)/4 = 20/4 = 5.0. It always returns a float. For the median (middle value), use statistics.median()." },
      { question: "What is the difference between variance() and pvariance()?", options: ["variance is for samples, pvariance for populations", "pvariance is for samples, variance for populations", "They are identical", "pvariance is faster"], correct: 0, explanation: "variance() uses n-1 (Bessel's correction) for SAMPLE data — when your data is a subset of a larger population. pvariance() uses n for POPULATION data — when you have the complete dataset. Sample variance is slightly larger to account for sampling uncertainty." },
      { question: "Why should you create Decimal from strings?", options: ["Decimal(0.1) inherits float imprecision", "Strings are faster", "Numbers aren't supported", "It's a Python requirement"], correct: 0, explanation: "Decimal(0.1) creates Decimal('0.1000000000000000055511151231257827021181583404541015625') because 0.1 is already an imprecise float before Decimal sees it. Decimal('0.1') creates an exact 0.1. Always use string input for precision." },
      { question: "What does Decimal('19.99').quantize(Decimal('0.1')) do?", options: ["Rounds to 1 decimal place: 20.0", "Returns 19.99", "Truncates to 19.9", "Raises an error"], correct: 0, explanation: "quantize() rounds a Decimal to match the exponent of the argument. Decimal('0.1') has 1 decimal place, so 19.99 rounds to 20.0 (using default ROUND_HALF_EVEN). You can specify rounding mode with the rounding parameter." },
      { question: "What does statistics.correlation() measure?", options: ["Linear relationship strength (-1 to 1)", "Average difference", "Percentage similarity", "Data spread"], correct: 0, explanation: "correlation() returns the Pearson correlation coefficient: a value from -1 to 1. +1 means perfect positive correlation, -1 means perfect negative, 0 means no linear relationship. Values above 0.7 or below -0.7 indicate strong correlation." },
      { question: "What does statistics.mode([1,2,2,3,3]) return in Python 3.8+?", options: ["2 (first mode encountered)", "Error: no unique mode", "[2, 3]", "2.5"], correct: 0, explanation: "In Python 3.8+, mode() returns the first mode encountered when there are ties. For all modes, use multimode() which returns [2, 3]. In Python 3.7, mode() raises StatisticsError when there's no unique mode." },
      { question: "What does linear_regression() return?", options: ["(slope, intercept) tuple", "A single prediction value", "A regression line plot", "R-squared value"], correct: 0, explanation: "linear_regression(x, y) returns a (slope, intercept) tuple for the line y = slope*x + intercept that best fits the data. Use these to predict: predicted_y = slope * new_x + intercept." },
      { question: "When should you use Decimal over float?", options: ["Financial calculations requiring exact precision", "Scientific computing", "Game physics", "All the time"], correct: 0, explanation: "Use Decimal for financial/monetary calculations where exact precision matters (banks, invoices, taxes). Float is fine for scientific computing, graphics, and games where tiny rounding errors are acceptable. Decimal is slower but exact." }
    ]
  },
  {
    id: 'qlib10', lessonId: 'lib10', title: 'dataclasses & enum Quiz', difficulty: 'library',
    questions: [
      { question: "What does @dataclass auto-generate?", options: ["__init__, __repr__, __eq__", "Only __init__", "__init__ and __str__", "All dunder methods"], correct: 0, explanation: "@dataclass automatically generates __init__() (constructor), __repr__() (string representation), and __eq__() (equality comparison) based on the class fields. You can also enable __hash__ and ordering with parameters." },
      { question: "Why use field(default_factory=list) instead of tags: list = []?", options: ["Prevents all instances sharing the same list", "It's faster", "Regular defaults don't work", "Python syntax requirement"], correct: 0, explanation: "Mutable defaults (like []) are shared across ALL instances — appending to one instance's list affects all others. field(default_factory=list) creates a NEW empty list for each instance. This is the same 'mutable default argument' problem from regular functions." },
      { question: "What does @dataclass(frozen=True) do?", options: ["Makes instances immutable (read-only)", "Improves performance", "Freezes the class definition", "Disables inheritance"], correct: 0, explanation: "frozen=True makes instances immutable — attempting to set attributes after creation raises FrozenInstanceError. This also auto-generates __hash__(), allowing frozen dataclasses to be used as dict keys and in sets." },
      { question: "What is __post_init__ used for?", options: ["Computed fields and validation after __init__", "Cleanup before deletion", "Post-processing output", "Registering the instance"], correct: 0, explanation: "__post_init__() runs right after the auto-generated __init__(). Use it for computed fields (like area from width*height), validation (raising errors for invalid values), or transformations (converting types)." },
      { question: "What is the advantage of Enum over string constants?", options: ["Type safety, auto-complete, no typos", "Better performance", "Less memory usage", "Required by Python 3"], correct: 0, explanation: "Enums prevent bugs: Status.ACTVE is a NameError caught immediately, but 'actve' is a valid string that silently causes logic errors. Enums also support iteration, comparison, and IDE auto-complete. They make code self-documenting." },
      { question: "What does auto() do in an Enum?", options: ["Automatically assigns incrementing values", "Auto-detects the type", "Creates automatic string values", "Randomly assigns values"], correct: 0, explanation: "auto() generates automatic values — by default, incrementing integers starting from 1. For IntEnum, auto() gives 1, 2, 3, etc. You can override _generate_next_value_() to customize the auto-generation logic." },
      { question: "How does IntEnum differ from regular Enum?", options: ["IntEnum values can be compared with integers", "IntEnum is faster", "IntEnum only accepts integers", "There is no difference"], correct: 0, explanation: "IntEnum members ARE integers — they support arithmetic and comparison with ints directly: Priority.HIGH > 2 works. Regular Enum members are NOT comparable with their values: Color.RED > 1 raises TypeError. Use IntEnum when you need numeric operations." },
      { question: "What does field(compare=False) do in a dataclass?", options: ["Excludes the field from __eq__ and ordering", "Makes the field non-comparable", "Hides the field from __repr__", "Makes the field optional"], correct: 0, explanation: "compare=False tells the auto-generated __eq__() and ordering methods to ignore this field. Two instances can be 'equal' even if this field differs. Useful for metadata fields like timestamps or IDs that shouldn't affect equality logic." }
    ]
  }
]
